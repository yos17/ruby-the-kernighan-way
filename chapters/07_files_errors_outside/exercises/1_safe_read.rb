# Exercise 1 — safe_read(path)
#
# Returns file contents or "" if the file doesn't exist or is unreadable.
# Logs a warning to STDERR on failure (not STDOUT — keep error output separate).

# TODO: def safe_read(path)
